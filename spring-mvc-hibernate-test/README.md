使用说明：

	配置application.properties文件（数据库信息），然后按照以下2种方式测试：

		测试1：导入Maven项目配置Server以及JDK，然后Run AS -> Run On Server启动项目即可看到效果；
	
		测试2：先执行数据库脚本employee.sql（例子中使用的是mysql数据库），
	                              然后执行com.workspace.spring.AppMain.java类的main函数；

